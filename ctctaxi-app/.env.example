# Form endpoint — booking, driver application and contact all post here.
# Works with Formspree, Netlify Forms, a Cloudflare Worker, or your own API.
# Without this the forms validate and show success but transmit nothing.
VITE_FORM_ENDPOINT=
